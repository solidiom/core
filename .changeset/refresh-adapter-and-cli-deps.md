---
"@solidiom/cli": patch
"@solidiom/adapter-virtualization-tanstack": patch
"@solidiom/adapter-date-internationalized": patch
---

Refresh third-party dependencies to their latest compatible releases.

- `@solidiom/cli`: bump `zod` to v4 (`z.record` now takes an explicit key schema) and `ts-morph` to v28. No change to CLI behavior or output.
- `@solidiom/adapter-virtualization-tanstack`: bump `@tanstack/virtual-core` to 3.17.8.
- `@solidiom/adapter-date-internationalized`: bump `@internationalized/date` to ^3.12.3.
