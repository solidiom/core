import type { JSX } from "solid-js"

export function AuditLog(): JSX.Element {
  return (
    <main class="min-h-screen p-6">
      <h1 class="text-2xl font-bold mb-6">Audit Log</h1>
      <p>Multi-tenant admin — searchable event timeline and activity log.</p>
    </main>
  )
}
