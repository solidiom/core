import type { JSX } from "solid-js"

export function Roles(): JSX.Element {
  return (
    <main class="min-h-screen p-6">
      <h1 class="text-2xl font-bold mb-6">Roles & Permissions</h1>
      <p>Identity & Access — define roles, assign permissions, and manage access policies.</p>
    </main>
  )
}
