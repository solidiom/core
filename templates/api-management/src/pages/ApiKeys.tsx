import type { JSX } from "solid-js"

export function ApiKeys(): JSX.Element {
  return (
    <main class="min-h-screen p-6">
      <h1 class="text-2xl font-bold mb-6">API Keys</h1>
      <p>API Management — create, rotate, and revoke API keys with scope and rate-limit policies.</p>
    </main>
  )
}
