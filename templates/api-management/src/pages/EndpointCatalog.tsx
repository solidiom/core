import type { JSX } from "solid-js"

export function EndpointCatalog(): JSX.Element {
  return (
    <main class="min-h-screen p-6">
      <h1 class="text-2xl font-bold mb-6">Endpoint Catalog</h1>
      <p>API Management — browse, search, and document API endpoints with versioning.</p>
    </main>
  )
}
