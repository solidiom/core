import type { JSX } from "solid-js"

export function Export(): JSX.Element {
  return (
    <main class="min-h-screen p-6">
      <h1 class="text-2xl font-bold mb-6">Export</h1>
      <p>Audit Log — export filtered audit data as CSV, JSON, or generate compliance reports.</p>
    </main>
  )
}
