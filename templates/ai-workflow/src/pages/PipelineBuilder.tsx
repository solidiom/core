import type { JSX } from "solid-js"

export function PipelineBuilder(): JSX.Element {
  return (
    <main class="min-h-screen p-6">
      <h1 class="text-2xl font-bold mb-6">Pipeline Builder</h1>
      <p>AI Workflow — visual pipeline builder for composing multi-step AI workflows.</p>
    </main>
  )
}
