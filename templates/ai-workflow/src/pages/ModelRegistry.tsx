import type { JSX } from "solid-js"

export function ModelRegistry(): JSX.Element {
  return (
    <main class="min-h-screen p-6">
      <h1 class="text-2xl font-bold mb-6">Model Registry</h1>
      <p>AI Workflow — manage, version, and configure AI models for pipeline steps.</p>
    </main>
  )
}
