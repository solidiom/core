import type { JSX } from "solid-js"

export function ExecutionLogs(): JSX.Element {
  return (
    <main class="min-h-screen p-6">
      <h1 class="text-2xl font-bold mb-6">Execution Logs</h1>
      <p>AI Workflow — monitor pipeline runs, inspect step outputs, and trace failures.</p>
    </main>
  )
}
