import type { JSX } from "solid-js"

export function Organization(): JSX.Element {
  return (
    <main class="min-h-screen p-6">
      <h1 class="text-2xl font-bold mb-6">Organization Settings</h1>
      <p>Enterprise Settings — configure organization profile, branding, and domain verification.</p>
    </main>
  )
}
