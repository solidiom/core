import type { JSX } from "solid-js"

export function Chat(): JSX.Element {
  return (
    <main class="min-h-screen p-6">
      <h1 class="text-2xl font-bold mb-6">Chat</h1>
      <p>AI chat — conversational interface with message history.</p>
    </main>
  )
}
