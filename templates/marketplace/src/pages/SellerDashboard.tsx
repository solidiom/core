import type { JSX } from "solid-js"

export function SellerDashboard(): JSX.Element {
  return (
    <main class="min-h-screen p-6">
      <h1 class="text-2xl font-bold mb-6">Seller Dashboard</h1>
      <p>Marketplace — manage listings, view sales analytics, and handle orders.</p>
    </main>
  )
}
