import type { JSX } from "solid-js"

export function Runs(): JSX.Element {
  return (
    <main class="min-h-screen p-6">
      <h1 class="text-2xl font-bold mb-6">Workflow Runs</h1>
      <p>Workflow Automation — monitor execution history with step-level logs and retry controls.</p>
    </main>
  )
}
