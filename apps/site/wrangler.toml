# OPS-004: Cloudflare Pages configuration for the Solidiom production site.
name = "solidiom-site"
compatibility_date = "2024-12-01"
pages_build_output_dir = "dist"

# Production routing and security configuration.
[env.production]
name = "solidiom-site"
