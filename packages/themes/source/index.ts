/**
 * @solidiom/themes — generated theme output package.
 *
 * `./css/<slug>.css` and `./tailwind/<slug>.css` are generated stylesheets (THEME-002,
 * THEME-003); this module exports lookup metadata only. There is no Solid component
 * surface here — a theme is CSS, not behavior.
 */
export { SHIPPED_THEMES, type ShippedTheme } from "./meta"
